# Placeholder — replace with production image when API is implemented
FROM python:3.11-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY advoi/ ./advoi/

RUN pip install --no-cache-dir .

EXPOSE 8000
CMD ["python", "-c", "print('advoi-api placeholder — implement HTTP server')"]