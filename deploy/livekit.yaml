# Self-hosted LiveKit — no LiveKit Cloud account required.
# Keys match `livekit-server --dev` (devkey / secret).
port: 7880
bind_addresses:
  - ""
keys:
  devkey: secret
rtc:
  tcp_port: 7881
  port_range_start: 50100
  port_range_end: 50200
  use_external_ip: true
room:
  auto_create: true